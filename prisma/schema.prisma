generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["views"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model snapshots_v1 {
  block_number   BigInt
  address        String  @db.Char(42)
  balance        String  @db.VarChar
  is_contract    Boolean
  is_blacklisted Boolean

  @@id([block_number, address], map: "snapshots_v1_pk")
  @@index([address], map: "snapshots_v1_address_index")
}
